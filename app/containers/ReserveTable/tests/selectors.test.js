// import { fromJS } from 'immutable';
// import { makeSelectReserveTableDomain } from '../selectors';

// const selector = makeSelectReserveTableDomain();

describe('makeSelectReserveTableDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
