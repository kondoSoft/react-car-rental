// import { fromJS } from 'immutable';
// import { makeSelectReserveDomain } from '../selectors';

// const selector = makeSelectReserveDomain();

describe('makeSelectReserveDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
