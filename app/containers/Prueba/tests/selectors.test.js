// import { fromJS } from 'immutable';
// import { makeSelectPruebaDomain } from '../selectors';

// const selector = makeSelectPruebaDomain();

describe('makeSelectPruebaDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
