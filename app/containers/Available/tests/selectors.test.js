// import { fromJS } from 'immutable';
// import { makeSelectAvailableDomain } from '../selectors';

// const selector = makeSelectAvailableDomain();

describe('makeSelectAvailableDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
