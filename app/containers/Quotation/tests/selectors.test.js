// import { fromJS } from 'immutable';
// import { makeSelectQuotationDomain } from '../selectors';

// const selector = makeSelectQuotationDomain();

describe('makeSelectQuotationDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
