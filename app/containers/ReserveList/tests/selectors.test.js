// import { fromJS } from 'immutable';
// import { makeSelectReserveListDomain } from '../selectors';

// const selector = makeSelectReserveListDomain();

describe('makeSelectReserveListDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
